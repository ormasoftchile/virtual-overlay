name: Build and Release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Setup WiX
      run: dotnet tool install --global wix && wix extension add WixToolset.Util.wixext

    - name: Create build directory
      run: |
        cmake -B build -G "Visual Studio 17 2022" -A x64

    - name: Build Release
      run: |
        msbuild build\virtual-overlay.sln /p:Configuration=Release /p:Platform=x64 /verbosity:minimal

    - name: Build MSI Installer
      working-directory: installer
      run: |
        wix build Package.wxs -ext WixToolset.Util.wixext -o VirtualOverlay.msi

    - name: Upload MSI Artifact
      uses: actions/upload-artifact@v4
      with:
        name: VirtualOverlay-Installer
        path: installer/VirtualOverlay.msi

  release:
    needs: build
    runs-on: windows-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    permissions:
      contents: write
    
    steps:
    - name: Download MSI
      uses: actions/download-artifact@v4
      with:
        name: VirtualOverlay-Installer

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          VirtualOverlay.msi
        generate_release_notes: true
